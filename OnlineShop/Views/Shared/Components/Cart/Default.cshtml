@model int

<a class="nav-link text-dark fw-bold" asp-controller="Cart" asp-action="Index">
    🛒 Корзина
    @if (Model > 0)
    {
        <span class="badge bg-danger rounded-pill text-white ms-1">@Model</span>
    }
</a>